{
  "name": "wargameplan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "fe": "lerna run --scope=react-frontend start",
    "be": "lerna run --scope=wargameplan-express start",
    "be-test": "lerna run --scope=wargameplan-express test",
    "fe-test": "lerna run --scope=react-frontend test",
    "test": "lerna run --parallel test",
    "all": "lerna run --parallel start"
  },
  "keywords": [],
  "author": "mikosramek",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
